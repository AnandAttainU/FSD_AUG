// 1. == is "equals to" operator to check both the operands are same or not
// 2. != is "not equals to" operator to check both the operands are different or not
// 3. > is "grater than" operator to check if left operand is greater or not
// 4. < is "lesser than" operator to check if left operand is less or not
// 5. >= is "greater than equal to" operator to check if left operand is greater or equal to or not
// 6. <= is "lesser than equal to" operator to check if left operand is lesser or equal to or not

// a = 10;
// b = "10";
// console.log(a==b); // == will only check the value
// console.log(a===b); // === will also check the type as well as value

// a = 5

// b = 6

// console.log(a++ + b++ + b-- + a--);

// X = 10 > 5; // true
// Y = 1 !== 5; // true
// console.log(Y);
// console.log("AND operator -- ", X && Y);
// console.log("OR operator -- ", X || Y);


// A program to check even or odd number
// num = 101;
// isEven = num%2 == 0; // false
// isOdd = num%2 != 0; // true
// console.log("The number is even:", isEven, "The number is odd:",  isOdd);

a = 10;
b = 100;
c = a < b;
d = 20000/20*100-123123123+123123123234;
console.log(c || d)
